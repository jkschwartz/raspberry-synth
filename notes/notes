Fixing Fluid Synth in MODEP


https://blokas.io/patchbox-os/docs/troubleshooting/

See logs sudo journalctl --unit modep-mod-host

Error when trying to add fluid synth based plugins like Fluid Pipes

Aug 31 16:38:16 patchbox mod-host[960]: lilv_lib_open(): error: Failed to open library /var/modep/lv2/FluidPipes.lv2/FluidPlug.so (/var/modep/lv2/FluidPipes.lv2/FluidPlug.so: ELF load command address/offset not page-aligned)
Aug 31 16:38:16 patchbox mod-host[960]: can't get lilv instance

sudo apt install lilv-utils

added kernel=kernel8.img to /boot/firmware/config.txt

what does that do? shrug something like this https://www.raspberrypi.com/documentation/computers/config_txt.html#kernel

then restart


Keybow2040 notes

https://learn.adafruit.com/welcome-to-circuitpython/advanced-serial-console-on-linux


Aeolus notes

sudo apt install aeolus

to make stop modifiable make stops writable

sudo chmod 777 -R /usr/share/aeolus/stops (maybe this should have a few fewer permissions?)


UI issues galore. Testing text mode. To enable a stop use this: s P + prin8


setBfree notes

sudo apt install setbfree (all lowercase)


Dexed notes

https://github.com/asb2m10/dexed?tab=readme-ov-file

Linux sampler (sfz)

https://linuxsampler.org/downloads.html

compile and install 

libgig
linuxsampler
gigedit
liblscp
qsampler

libgig svn co https://svn.linuxsampler.org/svn/libgig/trunk libgig

sudo apt install libtool
sudo apt install libsndfile1-dev
sudo apt install uuid-dev

make -f Makefile.cvs
./configure
make
sudo make install

svn co https://svn.linuxsampler.org/svn/linuxsampler/trunk linuxsampler


sudo apt install flex
sudo apt install bison

install libjack-jackd2-dev from ui package manager (add / remove software)

make -f Makefile.cvs
./configure
make
sudo make install

sfizz

build from source using these directions https://sfztools.github.io/sfizz/development/build/
default options 

add sfizz to path





